package printScreen.parser.verify
import printScreen.models.ast.AST

class verifyAST {

    // Esto lo que tiene que hacer es verificar que el AST
    // que se pase tenga bien el typado y que las cosas tengan sentido.

    fun verify (list : List<AST>) : Boolean {
        return false
    }
}